#include "problem2.h"
#include <iostream>
#include "math.h"
using std::cout;
using std::cin;
using std:: \n;

double function()
{
	cout << "Donne les valeurs de f(x) = sin(x) + ln(x) pour des valeurs de x de 1 a 10 :\n";
	for (double a = 0; a < 11; a++);
	{
		double f = sin(a) + log(a);
		cout << "f(x)="<< f <<"\n";
	}
	return 0;
}